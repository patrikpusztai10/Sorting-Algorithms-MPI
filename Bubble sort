def bubbleSort(array):
   length=len(array)-1
   for i in range(length):
      for j in range(length):
         if array[j]>array[j+1]:
            temp=array[j]
            array[j]=array[j+1]
            array[j+1]=temp
   return array
